# AI 生成内容规范

> 本文档约束所有 AI 辅助生成的博客内容，确保安全性、准确性和一致性。

---

## 一、信息安全红线

### 1.1 禁止出现的内容

以下信息**绝对不允许**出现在任何 AI 生成的文档中：

| 类别 | 示例 | 处理方式 |
|------|------|----------|
| 服务器 IP | `47.94.208.227` | 用 `<your-server-ip>` 替代 |
| 域名 | `door.dolovya.space` | 用 `<your-domain>` 替代 |
| 数据库连接串 | `mysql://user:pass@...` | 用占位符替代 |
| API 密钥 / Token | 任何形式的密钥 | 完全删除 |
| SSH 端口 / 服务器路径 | `/opt/door-clicker/` | 用 `<your-path>` 替代 |
| 个人服务器配置 | 真实 Nginx 域名、真实 systemd 用户名 | 用占位符替代 |

### 1.2 占位符规范

需要引用部署相关信息时，统一使用以下占位符：

```
<your-server-ip>      # 服务器 IP
<your-domain>         # 域名
<your-host>           # 主机地址
<your-path>           # 部署路径
<your-username>       # 系统用户名
<your-mqtt-broker>    # MQTT Broker 地址
```

### 1.3 允许保留的链接

- GitHub 仓库链接（`https://github.com/DoLovya/...`）
- 第三方工具官网链接（EMQX、PlatformIO 等）
- 文档引用链接

---

## 二、内容准确性要求

### 2.1 基于源码写作

- 所有技术描述**必须基于实际源码**，不能臆测
- 引脚、端口、文件名等需与代码一致
- 技术栈版本号需查 `requirements.txt`、`platformio.ini` 等配置文件
- 如果不确定，标注"需确认"

### 2.2 代码示例

- 代码示例需可直接运行或引用自项目源码
- 不编造不存在的 API 或函数
- 示例代码需标注来源（项目源码 / 示例代码）

---

## 三、文章结构规范

### 3.1 必备章节（共 12 条，不得缺省）

每篇技术博客**必须按以下顺序**包含以下章节（与 `BLOG-WRITING-GUIDE.md` 保持一致）：

```
 1. 项目背景 / 动机（为什么做）  ← 必写，见下文 §3.4 写作红线
 2. 项目简介 / 核心特性         # 一句话说明 + 核心特性列表
 3. 系统架构                    # 架构图 + 数据流说明
 4. 硬件准备（硬件类）/ 技术选型（软件类）
 5. 快速开始                    # 搭建步骤（可复现）
 6. 功能详解                    # 核心功能逐模块展开
 7. API 接口                    # 接口列表（如有）
 8. 部署上线                    # 部署配置示例（一律用占位符）
 9. 技术栈                      # 带版本号的技术清单（查 requirements.txt / platformio.ini 等）
10. 常见问题                    # FAQ，至少 3 条
11. 总结                        # 项目价值回顾
12. 相关链接                    # GitHub 源码仓库等
```

### 3.2 格式要求

- 标题层级：`#` 仅用于文章标题，`##` 用于章节，`###` 用于子节
- 表格：字段、类型、说明三列对齐
- 代码块：标注语言标签（`bash`、`python`、`json`、`nginx` 等）
- 图片引用：使用 jsDelivr CDN 链接 `https://cdn.jsdelivr.net/gh/DoLovya/blog@main/{path}`
- 提示框：`> ⚠️` 警告、`> 💡` 提示
- 图片 `<img>` 标签可以带 `width="70%"` ~ `width="85%"` 控制显示比例，禁止写死像素宽度

### 3.3 图片规范

- 命名：`{topic}-{description}.jpg`
- 尺寸：宽度不超过 1920px
- 格式：截图用 PNG/JPG，图表用 PNG
- 截图中不得包含工具覆盖层（如浏览器 Snapshot 标记）
- AI 生成图片仅用于示意图（架构图、流程图），界面截图用真实截图
- 图片统一存放于 `{项目名}/blog/images/`，禁止在 `blog/` 根目录平铺图片

### 3.4 「项目背景 / 动机」章节写作红线

AI 生成这一章节时**必须满足以下所有约束**，否则视为不合格：

1. **必须真实**：背景 / 动机来源于真实生活或工作场景，不得编造虚拟场景。
   - ✅ 可以：「住老式单元楼，每次朋友来要下楼按对讲机」「手头项目每天要手动跑 3 个命令，麻烦」
   - ❌ 禁止：「为了学习 XXX 我想做个项目」「最近看到很多人做这个所以我也做」
2. **≥ 2 个具体痛点**：每个痛点 ≥1 句话，讲清"麻烦在哪里"，不得写"现有方案不好用"等空话。
3. **必须交代核心思路**：必须写出「用什么技术栈 / 组件 + 解决了什么具体问题」，不能只列技术名词不说明用途。
4. **必须交代好处**：明确写出做完后改善了什么（≥1 句话）。
5. **禁用夸张字样**：颠覆、革命性、最强、完美、秒杀、吊打、宇宙第一、无懈可击等字样一律禁用。
6. **有实物图必须配图**：如果项目有真实安装 / 运行截图，背景章节必须至少插入 1 张（推荐用场景化的实物图，如硬件安装、运行效果）。
7. **字数控制**：200~400 字，短小精悍，不写散文式铺垫。

---

## 四、语言风格

- 技术博客风格，简洁直接
- 中文写作，代码注释用中文
- 不使用 emoji 装饰正文（核心特性列表、提示框除外）
- 叙述视角：项目背景 / 动机允许用第一人称（真实叙事需要），其余正文用客观第三人称描述
- 不使用夸张表述（"最强大的"、"完美的"等）

---

## 五、发布检查清单

发布前逐项检查：

- [ ] 无服务器 IP、域名、账号密码等敏感信息
- [ ] 所有占位符已正确替换为通用格式
- [ ] 技术描述与源码一致
- [ ] **「项目背景 / 动机」章节已写且满足 §3.4 写作红线**
- [ ] 12 个必备章节齐全
- [ ] 图片路径正确、无工具覆盖层，并且都在 `blog/images/` 下
- [ ] 代码块有语言标签
- [ ] 无编造的 API 或函数名
- [ ] GitHub 链接正确
