# AI 生成内容规范

> 本文档约束所有 AI 辅助生成的博客内容，确保安全性、准确性和一致性。

---

## 一、信息安全红线

### 1.1 禁止出现的内容

以下信息**绝对不允许**出现在任何 AI 生成的文档中：

| 类别 | 示例 | 处理方式 |
|------|------|----------|
| 服务器 IP | `47.94.208.227` | 用 `<your-server-ip>` 替代 |
| 域名 | `door.dolovya.space` | 用 `<your-domain>` 替代 |
| 数据库连接串 | `mysql://user:pass@...` | 用占位符替代 |
| API 密钥 / Token | 任何形式的密钥 | 完全删除 |
| SSH 端口 / 服务器路径 | `/opt/door-clicker/` | 用 `<your-path>` 替代 |
| 个人服务器配置 | 真实 Nginx 域名、真实 systemd 用户名 | 用占位符替代 |

### 1.2 占位符规范

需要引用部署相关信息时，统一使用以下占位符：

```
<your-server-ip>      # 服务器 IP
<your-domain>         # 域名
<your-host>           # 主机地址
<your-path>           # 部署路径
<your-username>       # 系统用户名
<your-mqtt-broker>    # MQTT Broker 地址
```

### 1.3 允许保留的链接

- GitHub 仓库链接（`https://github.com/DoLovya/...`）
- 第三方工具官网链接（EMQX、PlatformIO 等）
- 文档引用链接

---

## 二、内容准确性要求

### 2.1 基于源码写作

- 所有技术描述**必须基于实际源码**，不能臆测
- 引脚、端口、文件名等需与代码一致
- 技术栈版本号需查 `requirements.txt`、`platformio.ini` 等配置文件
- 如果不确定，标注"需确认"

### 2.2 代码示例

- 代码示例需可直接运行或引用自项目源码
- 不编造不存在的 API 或函数
- 示例代码需标注来源（项目源码 / 示例代码）

---

## 三、文章结构规范

### 3.1 必备章节

每篇技术博客应包含以下章节（可按需调整）：

```
1. 项目简介        # 一句话说明 + 核心特性列表
2. 系统架构        # 架构图 + 数据流说明
3. 硬件/环境准备   # 所需组件表 + 接线/环境说明
4. 快速开始        # 搭建步骤（可复现）
5. 功能详解        # 核心功能展开说明
6. API/接口        # 接口列表（如有）
7. 项目结构        # 目录树
8. 部署上线        # 部署配置示例（用占位符）
9. 技术栈          # 版本化技术清单
10. 常见问题       # FAQ
11. 总结           # 项目价值 + GitHub 链接
```

### 3.2 格式要求

- 标题层级：`#` 仅用于文章标题，`##` 用于章节，`###` 用于子节
- 表格：字段、类型、说明三列对齐
- 代码块：标注语言标签（`bash`、`python`、`json`、`nginx` 等）
- 图片引用：相对路径 `./xxx.jpg`，不使用绝对路径
- 提示框：`> ⚠️` 警告、`> 💡` 提示

### 3.3 图片规范

- 命名：`{topic}-{description}.jpg`
- 尺寸：宽度不超过 1920px
- 格式：截图用 PNG/JPG，图表用 PNG
- 截图中不得包含工具覆盖层（如浏览器 Snapshot 标记）
- AI 生成图片仅用于示意图（架构图、流程图），界面截图用真实截图

---

## 四、语言风格

- 技术博客风格，简洁直接
- 中文写作，代码注释用中文
- 不使用 emoji 装饰正文（核心特性列表除外）
- 不使用"我"字，用客观描述
- 不使用夸张表述（"最强大的"、"完美的"等）

---

## 五、发布检查清单

发布前逐项检查：

- [ ] 无服务器 IP、域名、账号密码等敏感信息
- [ ] 所有占位符已正确替换为通用格式
- [ ] 技术描述与源码一致
- [ ] 图片路径正确、无工具覆盖层
- [ ] 代码块有语言标签
- [ ] 无编造的 API 或函数名
- [ ] GitHub 链接正确
